{% extends '::base.html.twig' %}

{% block body -%}
    <h2>Lunch creation</h2>

    {{ form(form) }}

    <ul class="record_actions">
        <li>
            <a href="{{ path('ens_lunch') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}
