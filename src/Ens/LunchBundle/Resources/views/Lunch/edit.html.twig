{% extends '::base.html.twig' %}

{% block body -%}
    <h2>Lunch edit</h2>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('ens_lunch') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
