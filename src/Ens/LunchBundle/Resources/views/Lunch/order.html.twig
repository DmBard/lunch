{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Lunch list</h1>
    <table class="records_list">
        <thead>
        <tr>
            <th></th>
            <th></th>
            <th>Monday</th>
            <th>Tuesday</th>
            <th>Wednesday</th>
            <th>Thursday</th>
            <th>Friday</th>
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            {% for category in categories %}
                <tr>
                    <td> {{ user.username }}</td>
                    <td>{{ category }}</td>
                    {% for day in days %}
                        <td>
                            <label>
                                {% for lunch in lunches %}
                                    {% for join in joins %}
                                        {% if ((join.idUser == user.id) and (join.idLunch == lunch.id) and (lunch.day  ==  day ) and (lunch.categories  ==  category ) ) %}
                                            {{ lunch.description }}
                                        {% endif %}
                                    {% endfor %}
                                {% endfor %}
                            </label>
                        </td>
                    {% endfor %}
                </tr>
            {% endfor %}
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
